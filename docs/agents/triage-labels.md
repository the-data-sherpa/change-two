# Triage Labels

The engineering skills use five canonical triage roles. This repository uses the same string for each GitHub label.

| Canonical role | GitHub label | Meaning |
|---|---|---|
| `needs-triage` | `needs-triage` | A maintainer must evaluate the issue |
| `needs-info` | `needs-info` | The issue needs more information |
| `ready-for-agent` | `ready-for-agent` | The issue is fully specified for an agent |
| `ready-for-human` | `ready-for-human` | The issue requires human implementation |
| `wontfix` | `wontfix` | The issue will not be implemented |

When a skill names a canonical role, apply the corresponding GitHub label.
